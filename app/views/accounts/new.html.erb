<% content_for :title, "New account · terramanage" %>

<section class="narrow-page">
  <%= link_to "← Chart of accounts", accounts_path, class: "back-link" %>
  <div class="form-heading">
    <p class="eyebrow">Chart of accounts</p>
    <h1>New account</h1>
    <p>Add a main account or place it beneath an existing account.</p>
  </div>

  <div class="panel form-panel">
    <%= render "form", account: @account %>
  </div>
</section>
